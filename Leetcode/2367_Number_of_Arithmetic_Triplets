func arithmeticTriplets(nums []int, diff int) int {
   
    exists := make(map[int]bool)
    for _, num := range nums {
        exists[num] = true
    }

    count := 0
    
    for _, num := range nums {
        
        if exists[num-diff] && exists[num+diff] {
            count++
        }
    }

    return count
}