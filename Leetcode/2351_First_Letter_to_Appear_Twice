func repeatedCharacter(s string) byte {
    var mask int32 = 0
    for i := 0; i < len(s); i++ {
        charIdx := s[i] - 'a'
     
        if (mask >> charIdx) & 1 == 1 {
            return s[i]
        }
        
        mask |= (1 << charIdx)
    }
    return 0
}